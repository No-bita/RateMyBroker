{
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ts-node-dev": "^2.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "multer": "^2.0.0",
    "node-cron": "^4.0.7",
    "react-joyride": "^2.9.3",
    "recharts": "^2.15.3",
    "yahoo-finance2": "^2.13.3"
  }
}
